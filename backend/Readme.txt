Weather App: Build a weather app that allows users to search for a location and displays the current weather information. 
You can use a weather API (such as OpenWeatherMap) to fetch weather data from the backend server and display it in the 
React frontend.