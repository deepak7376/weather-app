{
  "name": "backend",
  "version": "1.0.0",
  "description": "Weather App: Build a weather app that allows users to search for a location and displays the current weather information.  You can use a weather API (such as OpenWeatherMap) to fetch weather data from the backend server and display it in the  React frontend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
